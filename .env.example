# ═══════════════════════════════════════════════════════════════════════════════
# ALTIORA AI - Environment Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values

# ─── Twilio Configuration ───────────────────────────────────────────────────────
TWILIO_ACCOUNT_SID=your_account_sid
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# ─── Server Configuration ───────────────────────────────────────────────────────
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
PUBLIC_URL=https://your-ngrok-url.ngrok-free.app

# ─── Pipeline Mode ──────────────────────────────────────────────────────────────
# Options:
#   mock       = Fake AI responses (for testing Twilio)
#   modal      = Use Modal serverless GPUs (recommended for production)
#   modal-edge = Modal for STT/LLM, Microsoft Edge TTS (free, faster TTS)
#   runpod     = Use RunPod endpoints (legacy)
PIPELINE_MODE=mock

# ─── Modal Configuration ────────────────────────────────────────────────────────
# Your Modal workspace (shown after running 'modal setup')
MODAL_WORKSPACE=neet18101

# Modal endpoints are auto-generated from workspace name
# Override only if you customized the function names:
# MODAL_STT_URL=https://neet18101--altiora-stt-transcribe.modal.run
# MODAL_LLM_URL=https://neet18101--altiora-llm-chat.modal.run
# MODAL_TTS_URL=https://neet18101--altiora-tts-synthesize.modal.run

# ─── LLM Configuration ──────────────────────────────────────────────────────────
LLM_MODEL=mistralai/Mistral-7B-Instruct-v0.3
LLM_SYSTEM_PROMPT=You are a helpful AI phone assistant. Keep responses short and conversational (1-2 sentences max). Be natural like a real human on the phone.

# ─── Voice Configuration ────────────────────────────────────────────────────────
# Supported: en, fr, es, ar
DEFAULT_LANGUAGE=en

# ─── Audio Processing ───────────────────────────────────────────────────────────
# Seconds of silence before processing (lower = faster response)
SILENCE_THRESHOLD=1.5
# Minimum audio bytes before processing
MIN_AUDIO_LENGTH=3200
# Energy threshold for barge-in detection
BARGE_IN_ENERGY_THRESHOLD=500

# ─── RunPod Configuration (Legacy) ──────────────────────────────────────────────
# Only needed if PIPELINE_MODE=runpod
# STT_API_URL=http://localhost:8001
# LLM_API_URL=http://localhost:8002/v1
# TTS_API_URL=http://localhost:8003
